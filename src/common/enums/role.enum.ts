/**
 * Role Enum
 * ---------
 * Defines the possible user roles in our application.
 * - USER: Regular user who can manage their own tasks
 * - ADMIN: Administrator who can manage all tasks
 */

export enum Role {
    USER = 'user',
    ADMIN = 'admin',
}
